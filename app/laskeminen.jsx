import { data } from "~/data.json";
import { getData } from "~/api.server";
import { laskemiseen } from "~/api.server";

const laskeminen(toiminto, kilowatit) {
    toiminto = laskemiseen.nimi;
    kilowatit = laskemiseen.kulutus;

}